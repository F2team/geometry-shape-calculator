export function calculateVolumeSphere(sphereRadius) {
  return (4 / 3) * Math.PI * sphereRadius ** 3;
}

export function calculateSurfaceAreaSphere(sphereRadius) {
  return 4 * Math.PI * sphereRadius ** 2;
}
